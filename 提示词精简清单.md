# 提示词精简清单

这一次把 `PROMPT.py` 从「4 个整段常量」改成了「具名片段 + `build_system_prompt(role)` 拼装」，
顺带删掉了 40 句。**这张单子记的就是那 40 句各去了哪** —— 将来真出问题，能回溯到是哪一刀。

## 判据（只有一条，是项目自己写的）

> **能在别处表达的，不进提示词。**

留下来的只有四类：**判断**（什么时候用 A 不用 B）、**跨工具的机制**（结果回流路径、
状态语义）、**护栏**、**报文格式**。接口细节归工具描述，权限归 `ROLE_DENIED`。

★ 但有一条反直觉的：**提示词里有一部分是对抗模型默认倾向的**。删了它不会报错、不会崩，
表现只是「模型开始做我原来不希望它做的事」，而且没人会想到是提示词少了一句。
所以判据的补充是：**删之前先问「删了之后模型会不会做出我原来不希望它做的事」，会 → 不删。**
护栏一个字都没动，见文末。

## 一、与工具描述重复的接口细节（32 句）

重复的方向是**「工具描述里已经写全了，提示词又抄了一遍」**，所以删提示词这一份不丢信息。
（逐条核过：24 个工具的描述共 3301 字符。）

| 删掉的句子 | 现在在哪 |
|---|---|
| 创建：用 job_create 建任务，触发方式二选一 | `job_create` 描述 |
| 周期任务用 schedule：用户说「每天9点…」「每小时…」「每周五…」时 | `job_create` 描述（逐字） |
| 五段式「分 时 日 月 周」，如 `'0 9 * * *'`（每天9:00）、`'*/30 * * * *'` | `job_create` 描述（逐字） |
| 一次性任务用 once_at：用户说「明天下午3点…」「下周一早上…提醒我」时 | `job_create` 描述（逐字） |
| 给具体时刻 `'YYYY-MM-DD HH:MM'` | `job_create` 描述（逐字） |
| 当前时间见系统提示词，跨天了就用 bash 跑 date 复核 | `job_create` 描述（含具体命令） |
| content 必须自包含：到点后是照着这句话执行，执行者手里没有此刻的对话上下文 | `job_create` 描述 |
| 一轮跑完后你什么都不用做；周期任务下次到点会自动重新派发（不需要你重新创建） | `job_create` 描述 |
| 一次性任务则就此结束 | `job_create` 描述 |
| 管理：job_list 查看全部任务（顺便拿 job_id） | `job_list` 描述 |
| job_cancel：取消，不再调度但保留记录 —— 之后可以用 job_resume 恢复 | `job_cancel` 描述 |
| job_delete：删除，记录一并移除，不可恢复 | `job_delete` 描述 |
| （删了就没法恢复，取消还可以恢复） | `job_delete` 描述 |
| `team_spawn(name, prompt)`：name 是成员的名字（自己起，如 'alice'，只用于展示） | `team_spawn` 描述 |
| prompt 是它的初始任务 | `team_spawn` 描述 |
| 工具会返回这个成员的 **ID**（形如 `alice-7f3a`） | `team_spawn` 描述 |
| 启动后成员立刻开始做 prompt 里的事 | `team_spawn` 描述 |
| 之后继续派活用 `send_message(to=ID, content=...)` | `team_spawn` 描述说「之后一律用这个 ID 给它发消息」（未点名工具，工具名在工具表里） |
| 团队成员自己不能再建团队 | `team_list` 描述：「成员只能查看，不能修改团队成员」 |
| `team_list`：看每个成员的 **ID \| 名字 \| 逻辑状态 \| 执行状态** | `team_list` 描述 |
| 逻辑状态 alive 在岗 / exiting 已请它下线、等它确认 / offline 已下线 / dead 线程异常终止 | `team_list` 描述 |
| 成员不会记错 ID，记不准就先 team_list 查一下 | `team_list` 描述 |
| `team_stop(member)`：member 填 ID（唯一的名字也可以） | `team_stop` 描述 |
| 这只是**发出下线请求**：成员会自己判断——手头还有活、还在等别人回复，它就**拒绝下线** | `team_stop` 描述 |
| 确认可以结束了才同意下线 | `team_stop` 描述 |
| 所以 team_stop 返回后**不要宣布"它已经退出了"** | `team_stop` 描述（逐字保留了这个告诫） |
| 它的状态先是 exiting，等它确认并真的停下来才会变成 offline | `team_stop` 描述 |
| 想确认结果可以用 team_list 看状态，或等它的回信 | `team_stop` 描述 |
| 系统按 cron 表达式把到点的任务放进队列／全程不需要你参与，你只负责创建和管理 | 压缩后留在 `_SCHEDULE` 开头 |
| ### 请成员下线（team_stop） | 标题，排版 |

## 二、结构重组后不再需要（3 句）

这三句**不是被删掉，是切片之后没有存在必要了** —— 它们是「先给所有人主agent那份说明，
再补一句『上面那些不适用于你』」这个附录式写法的产物。现在成员根本看不到那一段，
纠偏的话也就无从说起。

| 句子 | 为什么不需要了 |
|---|---|
| 上面关于「Agent Teams / subagent」的说明适用于主agent | 成员压根拿不到那一段（`_ROLE_PARTS` 不给），不需要声明适用范围 |
| 你**不能**创建团队成员，也不能启动 subagent | 改写成 `_TEAM_LIFE` 的「不能派下级agent」—— **规则照说，但不用工具名说**：点名一个它没有的工具正是 `check_prompts` 要抓的坏味道 |
| 可以自己做的就自己做，需要人手就把需求说给主agent | 逐字进了 `_TEAM_LIFE`，不是删除 |

## 三、提示词内部前后重复（1 句）

| 句子 | 为什么删 |
|---|---|
| 简单计算、单步工具调用、简单文件操作 | 同一份「简单的别派活」清单在「SubAgent 使用规则」和「Agent Teams」各写了一遍。新 `_DELEGATE` 只留一份 |

## 四、压缩改写、事实保留（4 句）

这几句在 diff 里看着像删了，其实是**同一件事换了个更短的说法**，事实一个字没丢。
列出来是为了让这张单子经得起对照 —— 不然下次有人拿 sents-diff 一比，会以为丢了东西。

| 旧的写法 | 新的写法 |
|---|---|
| 你是主Agent：负责理解用户需求 | `_IDENTITY_MAIN`：「你的职责是理解用户任务，规划执行流程」 |
| 向用户汇报最终结果 | `_DELEGATE` 的「### 你的职责」末条：「最后向用户汇总结果」 |
| SubAgent：负责执行被分配的具体任务 | `_DELEGATE`：「subagent 是**一次性**的：派活 → 阻塞等结果 → 用完即销毁」 |
| 系统按 cron 表达式把到点的任务放进队列… | `_SCHEDULE`：「定时任务到点后由**派发器**在独立的后台 agent 里执行，全程不需要你参与」 |

## 没删的东西：护栏清单

`git log` 挖出来的、花过实测代价的句子，**一条都没动**。它们被 `_a2_spike/probe_prompt.py`
逐条钉死（那台探针最主要的用途就是这个），删一条立刻红：

| 句子 | 来历 |
|---|---|
| 长期记忆由主agent统一维护：需要记住什么，说给主agent，你自己不写（成员 + 子agent各一条） | `9b37de0` 实测踩出来的：成员要写记忆、没有 `write_memory`，于是自己用 bash 调 `memory.py` 的 handler 绕过 |
| 不要谎报结果 | 测试报告·问题6：成员谎报「已启动成员」 |
| 不要发没有信息量的消息 | 成员之间互相刷「喵~」，靠这条压住 |
| 拒绝下线并说明理由 | 下线握手必须由成员自己判断 |
| 不要直接相信 subagent 声明完成 | 结果处理段的全部价值 |
| 不要谎称看到了执行过程 | `b0c0e79` 加的，防幻觉 |
| content 里别写需要用户确认的动作 | `b0c0e79` 加的：任务跑在后台、没人在终端 |
| 也不要用 bash sleep 去等 | 反浪费：发消息是异步的 |
| 不要滥用subagent | 简单任务别派活 |

这些句子没有一条能靠 `TOOLS.py` 表达 —— 工具描述说的是「怎么调」，它们说的是
「**别**用默认倾向去做那件事」。

## 怎么验的

| 层 | 是什么 | 这次的结果 |
|---|---|---|
| L0 | `claude.py` 启动自检：三个角色的提示词点名的工具必须在它自己手里 | 重构前 **三个角色里两个报错**（成员点名 8 个它没有的工具、子agent 10 个）；重构后**全绿** |
| L1 | `_a2_spike/probe_prompt.py`：护栏清单、角色隔离、共享片段、反向、长度棘轮 | 全绿 |
| L2 | `_a2_spike/l2_prompt_ab.py`：8 条真模型行为用例 × 3 次，改前改后各跑一遍 | **没有一条从通过变失败**；case 4（定时任务防幻觉）2/3 → 3/3 |

L2 的判据是「没有一条从通过变失败」，不是「全过」—— 基线本来就有做不到的条目。
