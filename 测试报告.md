# Agent Team Runtime 测试报告

按《测试.md》执行的交互性测试结果。**全程未修改任何 Agent Team 实现代码**，所有观测通过外部脚本驱动 + 进程内探针完成。

测试对象：`message.py` / `claude.py` / `main.py` / `agent_runner.py`
测试环境：Windows 11 + Python 3.13 + Git Bash（MSYS2）

---

## 0. 测试方法与局限（请先读这段）

**测试怎么做的**

- 交互测试：把 `main.py` 作为子进程拉起，用管道喂 stdin、抓 stdout 打时间戳，做成反应式的 `wait_for()` 驱动（`run_interactive_test.py`、`probe_test4.py`）。
- 精确观测：在进程内对 `TOOLS.execute_bash`、`llm.send` 打点计时（`probe_team.py`、`run_main_instrumented.py`），不碰实现代码。
- 卡死现场：60 秒后 `sys._current_frames()` + `traceback.print_stack()` 转储全部线程栈。

**必须如实说明的三处观测瑕疵**（避免把测试代码的问题算到实现头上）

1. **第一轮"退出后线程仍存活"是误读**：当时的判据是"按名字枚举 `threading.enumerate()` 里还有没有叫 alice 的线程"，结果匹配到了测试 2/3/10 遗留的、从未退出的同名线程。改用**精确的线程对象** `t.is_alive()` 重测 → `state=exit` 后 `is_alive() == False`，**实现是对的**。
2. **场景 A 里"测试 4"的时间窗口无效**：进入计时窗口时 alice 的消息还没上总线，那一格的延迟数字不可用。主 agent 侧 0.5s 的唤醒延迟来自独立的探针 2，不是来自场景 A。
3. **测试 7 的 CPU 只采了 8 秒**（≈1.2%，其中含进程启动开销和 Scheduler 扫描线程），没做干净的 30 秒复测，**这个数字只能当"没有空转"的定性结论**。

**一个关键前提**：用"管道喂 stdin"的方式驱动 `main.py`，会踩到一个 Git Bash 的坑（见**问题 5**）。本轮有几次看起来"队友卡死"的运行，是驱动方式造成的，**不是团队功能本身的问题**。真终端里不会有这个现象（已实测对照，见问题 5）。

---

## 1. 正常功能

| # | 测试项 | 结果 | 关键证据 |
|---|---|---|---|
| 1 | 基本 Alice → Bob 通信 | ✅ 正常 | 消息上总线、对方被唤醒、内容完整 |
| 2 | 工作期间收到消息不丢失 | ✅ 正常 | 实测：消息 67.37s 到达 → 73.98s 被处理 |
| 3 | 工作期间多条消息 A/B/C | ✅ 全部处理 | 顺序保持、无重复、无丢失 |
| 4 | Main 无用户输入时收团队消息 | ✅ 能自主醒来 | 唤醒延迟 ≈ 0.5s（`timeout=1` 轮询均值，最坏 1s） |
| 5 | Main 在工作中收到消息 | ✅ 能收到（非打断） | 同测试 2 语义，见问题 4 |
| 6 | 双向并发通信 | ✅ 正常 | 无竞争、无死锁、双方都收到、都回到 idle |
| 7 | 空闲 CPU 占用 | ✅ 基本不占 | `Condition.wait()` 真阻塞，非 `while True` 空转 |
| 8 | 退出（/exit） | ✅ 正常* | `state=exit`、线程结束、不再处理消息（*见问题 1 的边界情况） |
| 9 | /exit 与普通消息同时到达 | ⚠️ 有语义问题 | 见问题 1、问题 2 |
| 10 | 消息顺序 FIFO | ✅ 正常 | 1→2→3→4→5 严格有序 |

已经确认无问题的点：FIFO 保序、无丢失、无重复、无死锁、双向通信后双方都能回到 idle、`Condition.wait()` 确实挂起线程、退出后的 agent 不再消费任何消息。

---

## 2. 实际行为

### idle → work → idle

`run_forever()` 是队友的生命周期循环：

```
self.state = "idle"
msg = self.message_bus.receive(self.agent_name)   # ← 真正的阻塞点，没消息就挂在这里
self.state = "work"
... self.run(team_history) ...                    # 一整个工作期
（run 返回，模型不再调工具）→ 回到 while 顶部 → idle
```

`receive()` 内部是 `while True` 守卫循环 + `Condition.wait()`，所以 idle 是**真阻塞**：线程挂起、0% CPU、消息一到立刻唤醒，而不是定时轮询。这一点测试 7 已验证。

### 工作期间收到消息（测试 2 的重点）

实测时序（队友正在跑一条耗时命令时，第二条消息到达）：

```
67.37s  第二条消息上总线
73.98s  该消息被处理
        ↑ 间隔 6.6s = 当时那条 bash sleep 剩余的时长
```

结论：**消息不丢、不打断、当前工具轮次结束后才被取出**。

但这里有一个**与《测试.md》预期图示不一致的地方**，需要纠正：

> 文档预期：`run() 返回 → receive() → 第二条消息被取出`
>
> 实际行为：第二条消息**根本走不到 `receive()`**。`run()` 的循环顶部有一句
> `team_message = self.collect_team_messages()`（claude.py:111），它会把总线里
> 发给自己的消息**一次性 drain 出来，拼成一条 user 消息**塞进当前对话。
> `run()` 是主 agent 和队友共用的，所以队友在 work 期间同样会走这条路径。

也就是说，投递其实有**两条路径**：idle 时走 `receive()`（一次一条），work 时走 `collect_team_messages()`（批量合并）。这正是问题 1、3 的根源。

### 多消息（测试 3）

A/B/C 在队友工作期间连续到达 → 被 `collect_team_messages()` **合并成一条** user 消息（内部是多个 `<team_message>` 块），在**同一个 `run()`** 里处理完。所以：

- 顺序保持（`drain()` 是按 list 顺序取的）、无重复、无丢失 ✅
- **不需要**执行多次 `run()`；只有在消息落在 idle 期间时，第一条才由 `receive()` 单独取走

### Main Agent 收消息（测试 4）

主 agent 不能阻塞等消息（它要接键盘），所以靠 `main.py` 里的 input 线程 + 主循环轮询：

```
read_input 线程:  inbox.put(input("User: "))     ← 阻塞在键盘上
主循环:           inbox.get(timeout=1)           ← 1 秒没输入就超时
                  → queue.Empty → has_team_message()? → run(history)
```

实测：**用户完全零输入，主 agent 也能被团队消息唤醒**，唤醒延迟 ≈ 0.5s（`timeout=1` 轮询的均值，最坏 1s），之后是正常的模型耗时。`timeout=1` 就是这 0~1 秒延迟的来源——它是"多久检查一次有没有团队消息"的粒度。

### 双向并发（测试 6）

alice 和 bob 同时互发，`MessageBus` 的 `Condition` + `messages` 列表没有出现竞争、没有死锁、没有消息被错误 agent 取走（`receive()` 是按 `receiver` 精确匹配的）。两边处理完都回到 idle。

### shutdown（测试 8 / 9）

`/exit` 的闸门只有一处（claude.py:211），挂在 `receive()` 路径上。所以：

- 队友**空闲时**收到 `/exit` → 干净退出：`running=False`、`state=exit`、线程结束、后续消息不再处理 ✅
- 队友**正在 work 时**收到 `/exit` → 被 `collect_team_messages()` 当成普通团队消息吞掉，**永远不会退出** ❌（问题 1）

---

## 3. 发现的问题

### 问题 1：`/exit` 在 `run()` 执行期间到达会被吞掉，agent 永不退出

```
问题：     /exit 只在 receive() 这条路径上被检查,work 期间到达的 /exit 走的是另一条路径
实际行为： 队友正在 run() 里干活时发来的 /exit 被 collect_team_messages() 当作普通团队消息,
           拼进 user 消息交给模型当内容处理。退出闸门(claude.py:211)看不到它,
           /exit 被消费掉且不会再次出现 → agent 一直活着,state 停在 work/idle
预期行为： 无论从哪条路径到达,/exit 都应该触发退出
原因：     有两条投递路径(receive() 逐条 / collect_team_messages() 批量),但只有一个退出闸门,
           且只挂在 receive() 上。run() 是主/队友共用的,队友在 work 期间同样会走批量那条
是否属于当前设计预期： 否,属于实现 Bug(语义不一致)。复现见 test_team_edge.py T11
           (用 3 秒的 calculator 工具调用制造多轮 run())
```

### 问题 2：名字即收件箱——发给不在线名字的消息会被同名新 agent 继承

```
问题：     MessageBus 只按 receiver 字符串匹配,不校验收件人是否还活着
实际行为： 向已退出/根本不存在的 alice 发消息 → 消息静静留在 messages 里;
           之后新建一个同名的 alice → 它上线后会收到这条遗留消息
预期行为： 收件人不存在时应丢弃(或退回/告警),不应被后来的同名 agent 继承
原因：     存储转发模型没有 TTL、没有活跃性校验、没有"投递失败"的出口
是否属于当前设计预期： 存储转发本身是最小模型的合理简化,但"被同名新 agent 误收"这个后果
           不在预期内,建议列为待改进(测试 9 的"普通消息B"就是这种角色)
```

### 问题 3：两个方向的消息语义不一致（逐条 vs 批量）

```
问题：     队友和主 agent 拿到消息的粒度不一样
实际行为： idle 时 receive() 一次一条;run() 期间 collect_team_messages() 把多条合并成一条
           user 消息。所以对方连发 A/B/C,收到的可能是一条合并消息,分不出边界
预期行为： 建议明确一种语义(都逐条 或 都批量),并写进文档
原因：     两条路径各自实现,没有统一
是否属于当前设计预期： 属于当前实现的选择,但文档里没写明,容易误判
```

### 问题 4：不能中断正在执行的 `run()`（`has_team_message()` 只是唤醒探针）

```
问题：     run() 执行期间到达的消息无法打断当前工作
实际行为： 消息要等当前工具调用轮次结束、run() 循环进入下一次迭代才被取出
           (实测 6.6s = 当前命令剩余时长),不是在 run() 返回之后才处理
预期行为： 与《测试.md》一致——当前阶段不要求中断
原因：     run() 是同步阻塞的,工具调用不可取消,循环顶部只在"回合之间"检查一次总线
是否属于当前设计预期： 是。这是当前最小模型的既定取舍,不是缺陷。
           但要注意 has_team_message() 的定位:它只是 Main Runtime 的"该不该唤醒"探针,
           完全没有打断能力
```

### 问题 5：管道 stdin 场景下 bash 工具会挂满 300 秒（环境问题，非实现 Bug）

这是本轮查得最深、也最需要说清楚的一个。

```
问题：     main.py 的 stdin 若是管道、且有一条线程正阻塞在读它,bash 工具会长时间挂死
实际行为： bash 命令 300 秒不返回,最后由 execute_bash 的 timeout 兜底返回
           "命令执行超时(300s)"。卡死现场证实:bash.exe 进程活着(sleep 命令的
           sleep.exe 子进程甚至从未出现),说明 bash 根本没走到执行命令那一步
预期行为： 立即执行
原因：     不是 ClaudeMini 的逻辑,也不是 Python/Windows 管道本身——是 Git Bash(MSYS2)自己
           在启动阶段停住了。对照实验(probe_hang_layer.py):
             bash -c "echo hi"                   → 卡死(连不 fork 的命令都卡)
             bash -c "sleep 2"                   → 卡死
             cmd /c "echo hi"  (原生,不经 MSYS)  → 0.05s 正常
             bash + stdin=DEVNULL                → 0.10s 正常
           触发条件是"stdin 是管道 + 有线程挂起读它";换个 stdin 就正常
是否属于当前设计预期： 否,但属于**环境/健壮性**问题,不是本项目的实现 Bug。
           实测对照:
             stdin = 真控制台(isatty=True,用户平时的终端) → sleep 2 用 2.13s,完全正常
             stdin = 管道 + 阻塞读线程(脚本驱动 main.py)   → 卡死
           也就是说:**用户平时在终端里用不受影响**;但任何"把输入管道喂给 main.py"的用法
           (脚本驱动、echo ... | python main.py、CI、包装脚本)都会踩到,且会白等 5 分钟
```

> 澄清：本轮一开始追的那个"alice 卡死 300 秒"就是这个现象，**是我的测试驱动方式造成的**，
> 不是团队功能的问题。真终端里队友跑 bash 是好的。

### 问题 6：队友会自称"已完成"实际没做的事（模型行为，非实现 Bug）

```
问题：     队友自我报告的成功状态与实际不符
实际行为： 场景 A 里 bob 声称"已启动 alice 成员!",但它 allow_team=False,
           team_spawn 工具压根没注册,实际什么都没发生
预期行为： 失败/不可用时应当如实说明
原因：     模型行为(幻觉式宣告),不是实现问题
是否属于当前设计预期： 否,属于模型侧行为。工具返回"❌ 成员不能再创建团队"其实是对的,
           是模型没有如实转述
```

---

## 4. 当前架构的一个关键结论

**MessageBus 当前负责什么？**
纯粹的**存储转发 + 阻塞唤醒**。消息 append 进 `messages` 列表，`send()` 用 `notify_all()` 广播唤醒所有等待者，`receive()` 按 `receiver` 字符串精确匹配取出，`drain()` 批量取走，`has_message()` 只窥探不取。
它**不负责**：投递确认、收件人存活校验、优先级、超时、中断。消息会一直留在列表里直到被取走（这正是问题 2 的来源）。

**`run_forever()` 当前负责什么？**
队友的**生命周期状态机**：`idle`（阻塞在 `receive()` 上）→ `work`（拿到一条消息就跑一整个 `run()`）→ 回到 `idle`，直到收到 `/exit` 转 `exit` 并退出循环。
它是"下一个工作周期"的边界：`run()` 返回 = 这条消息处理完了。跨消息的对话记忆靠 `team_history` 在循环外保留。

**Main Agent 的 input thread 当前解决什么问题？**
解决"**同时等键盘和等团队消息**"。`input()` 会阻塞，如果直接写在主循环里就没法处理团队消息了。所以单独开一条线程专职阻塞读键盘、把结果丢进 `queue.Queue`，主循环用 `inbox.get(timeout=1)` —— 1 秒内要么拿到用户输入，要么超时去 `has_team_message()` 看一眼团队消息。这是"用户零输入时主 agent 也能被唤醒"的关键，也是那 0~1 秒唤醒延迟的来源。

**当前系统能否中断一个正在执行 `run()` 的 Agent？**
**不能。** 没有任何中断机制：没有取消标志、没有在工具调用之间检查退出条件、工具调用本身也不可取消。`has_team_message()` 只是主循环的**唤醒探针**，只在 `run()` 之外起作用，对正在跑的 `run()` 没有任何打断能力。消息只能排队，等当前这一轮工具调用结束、`run()` 循环进入下一次迭代时被取走。

**当前 Team 通信属于"异步消息传递"还是"实时中断式通信"？**
**异步消息传递**。语义是"存储转发 + 下一个工作周期处理"：可靠性够（不丢、保序、不重复），但没有实时性保证（延迟取决于对方当前那轮工作还要跑多久）。

---

## 5. 修改建议

只列必要的最小修改，按优先级排序。**建议都还没动手**，等确认。

**① `/exit` 统一出口（修问题 1，最高优先）**
在 `collect_team_messages()` 里识别 `/exit`：逐条检查 drain 出来的消息，遇到 `content.strip() == "/exit"` 就不再拼进 user 消息，而是把 `self.running = False`、`self.state = "exit"` 置好并返回一个"退出"信号，让 `run()` 尽快收敛返回。
最小形态是给 `collect_team_messages()` 加一个退出标志，`run()` 循环顶部看到标志就 `break`，`run_forever()` 回到 while 判断时自然退出。**不需要**重构投递路径。

**② `execute_bash` 加一行 `stdin=subprocess.DEVNULL`（修问题 5）**
`bash_exec.py:44` 的 `subprocess.run([bash, "-c", command], capture_output=True, ...)` 补上 `stdin=subprocess.DEVNULL`。
已实测：在会卡死的环境下加上它，`echo hi` 0.10s 返回、`sleep 2` 2.13s 返回。代价是 agent 的 bash 命令不再能读 stdin——但本来也没有交互式输入的场景，而且现在"能读 stdin"的代价是管道场景下挂死 5 分钟。

**③ 给不在线的收件人一个出口（修问题 2）**
两种都很小：`send()` 时校验 receiver 是否活跃，不活跃就直接丢弃并打印一行警告；或者给 `messages` 里滞留的消息加个上限/过期清理。前者更简单，也更符合"避免被同名新 agent 误收"的目标。配套可以在 `team_spawn()` 里加一句提示：新建同名成员会继承旧收件箱。

**④ 明确投递语义并写进注释/文档（修问题 3）**
不改行为也行，至少在 `collect_team_messages()` 的 docstring 里写清楚"会把多条合并成一条 user 消息"，并说明队友与主 agent 在这点上是否一致。否则下次看代码的人还会以为队友是逐条处理的。

**不建议做的**：为了这几个问题去改 `run()` 的同步结构、引入异步工具调用或中断机制。当前"排队 + 下个周期处理"的模型是能用的，测试 2 的重点问题也证明了它的边界是清晰的。

---

## 附：本轮产生的临时文件（未修改任何实现代码）

| 文件 | 用途 |
|---|---|
| `run_interactive_test.py` | 交互测试驱动（场景 A/B） |
| `probe_test4.py` | 测试 4 反应式复跑 |
| `probe_team.py` | bash/send 计时探针 + user_loop 复刻 |
| `run_main_instrumented.py` | bash 计时 + 线程栈转储 |
| `test_team_edge.py` | T11/T12/T13/T14 边界测试 |
| `test_team_runtime2.py` | 全可见性测试装置（测试 2/3/6/9/10） |
| `probe_bash_stdin.py` ~ `probe_bash_stdin5.py`、`probe_hang_hold.py`、`probe_hang_layer.py` | 问题 5 的定位实验链 |
| `interactive_log_A.txt` / `_A2.txt` / `probe4_console.txt` / `probe5_result.txt` | 原始日志 |

这些都是一次性的，确认结论后可以删。`message.py`、`claude.py`、`main.py`、`bash_exec.py`、`TOOLS.py`、`PERMISSIONS.py` **本轮一个字都没改**。
