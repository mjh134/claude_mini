# Agent Team 生命周期与成员管理设计

## 一、目标

本次改造只解决当前 Team 机制中的两个核心问题：

1. `/exit` 作为字符串消息可能被普通消息消费逻辑吞掉，导致 Agent 无法正确退出。
2. `agent name` 作为身份标识存在重复、复用以及旧消息被新同名 Agent 错误接收的问题。

同时完善 Team 成员的生命周期管理，使 Team 成员与普通一次性 Subagent 的生命周期语义有所区别。

**注意：具体代码结构、类、方法、线程同步方式、消息队列实现方式由你自行决定。本文只约束架构职责和行为语义，不限制具体实现。**

---

# 二、核心设计原则

### 1. Team 成员完成当前任务后不会自动退出

Team 成员与普通 Subagent 不同。

一个成员完成当前任务后：

```text
完成任务
    ↓
向 Main 汇报结果
    ↓
继续保持 alive / idle
    ↓
等待新的消息或任务
```

不能因为：

* 初始任务完成
* 当前没有消息
* 当前没有事情可做

就自动结束线程。

Agent 可以长期处于 idle，并继续等待后续协作。

---

### 2. 只有 Main Agent 可以决定发起成员下线

Team 成员没有自主决定生命周期结束的权力。

也就是说：

```text
Agent：
“我的任务完成了”
```

只能代表：

> 当前工作阶段完成。

不能代表：

> 我要退出 Team。

真正的下线由 Main Agent 根据整个 Team 当前任务和协作情况判断。

---

### 3. Main 发起下线请求，但不直接假设线程已经死亡

Main 判断某个成员当前已经不再需要后：

```text
Main → Agent：请求下线
```

Agent 收到后不能立即关闭线程，而要进行最后一次确认。

Agent 需要判断：

* 自己当前是否还有需要处理的工作
* 是否存在需要自己处理的新消息
* 当前是否仍存在必须完成的协作

如果 Agent 认为仍需要工作：

```text
Agent → Main：拒绝/暂不同意下线
```

继续保持 alive。

如果 Agent 确认当前可以结束：

```text
Agent → Main：同意下线
```

然后停止自己的运行循环和线程。

Main 在收到 Agent 的最终确认后，再同步更新成员的逻辑状态。

因此：

```text
Main 判断“不再需要”
        ↓
发送下线请求
        ↓
Agent 最终确认
        ↓
Agent 实际停止
        ↓
Main 更新成员状态
```

**逻辑状态不能仅仅因为 Main 发出了下线请求就直接变成 offline/dead。**

---

# 三、Team Runtime / Member Registry

新增一个成员管理层，由 Main Agent / Team Runtime 持有。

它负责管理 Team 成员的信息和生命周期。

成员至少需要：

```text
id
name（可选，但可以保留用于展示）
status
thread / Agent 实例引用（如实现需要）
```

其中：

### id

作为真正的 Agent 身份。

要求：

* 唯一
* 不重复
* Agent 生命周期结束后不重新分配给其他 Agent

可以使用：

```text
name + 短随机字符串
```

也可以使用纯随机 ID。

具体 ID 生成方式不做强制限制。

重要的是：

> **通信身份不能再依赖 name。**

---

### name

如果保留，仅作为：

* 展示
* 日志
* 模型理解

等用途。

不能作为唯一身份。

允许出现：

```text
alice-xxxx
alice-yyyy
```

这样的多个同名成员。

---

### status

表示 Team Runtime 所维护的**逻辑生命周期状态**。

状态设计不要求严格采用某几个固定字符串，但必须能够区分：

```text
成员正常存在/运行
成员正在退出
成员已经退出
成员异常死亡（如果实现需要）
```

尤其需要区分：

> “Main 希望 Agent 退出”

和：

> “Agent 的线程实际上已经退出”。

---

# 四、逻辑状态与物理线程状态分离

这是本次设计的重要原则。

例如：

```text
Member.status = alive
thread.is_alive() = False
```

两者可能暂时不一致。

因此：

* **Team Runtime / Main 管理逻辑状态**
* **Agent 自己负责执行实际线程退出**
* Runtime 可以通过线程状态进行必要的对账

不能把：

```text
status = offline
```

简单理解成：

```text
线程已经停止
```

最终 offline 状态应当建立在 Agent 已经实际完成退出的基础上。

---

# 五、MessageBus 的职责边界

MessageBus 仍然只是通信基础设施。

它负责：

```text
消息发送
消息存储
消息接收
等待消息
线程安全
```

不负责：

```text
Agent 生命周期决策
创建 Agent
销毁 Agent
决定某 Agent 是否应该退出
修改 Team 成员状态
```

成员管理属于 Team Runtime / Registry。

不要因为本次改造而把 MessageBus 变成 Team Manager。

---

# 六、消息身份与消息类型

Message 应当能够明确区分：

```text
发送者 ID
接收者 ID
消息类型
消息内容
```

至少需要区分：

### 普通业务消息

例如：

```text
Agent A → Agent B：
帮我确认这个 Controller 的权限问题。
```

这种消息交给 Agent 的正常工作流程 / LLM。

---

### 生命周期控制消息

例如：

```text
Main → Agent A：
请求下线。
```

这种消息属于 Team Runtime 的控制面。

不能再依赖：

```text
content == "/exit"
```

这种字符串约定判断生命周期控制。

控制消息不应该被错误地当成普通用户消息发送给 LLM。

具体控制消息类型以及名称由实现决定，但必须能够在消息处理层明确分流：

```text
普通消息 → Agent 工作逻辑
控制消息 → Agent 生命周期逻辑
```

---

# 七、消息寻址

发送消息时使用 Agent ID，而不是 name。

例如：

```text
sender_id = agent-A
receiver_id = agent-B
```

而不是：

```text
sender = "alice"
receiver = "bob"
```

发送消息之前应当能够确认目标成员属于当前 Team。

对于：

```text
不存在的 Agent
已经退出的 Agent
不再允许接收消息的 Agent
```

应当有明确处理方式，不能让消息无限制地堆积成未来可能被其他 Agent 错误消费的“遗留消息”。

尤其需要保证：

> Agent A 退出后，未来创建的新 Agent B 不会因为与 A 同名而收到 A 的历史消息。

---

# 八、Agent 的正常生命周期

正常 Team 成员生命周期应当是：

```text
创建
 ↓
运行
 ↓
完成当前任务
 ↓
汇报结果
 ↓
idle
 ↓
等待新的消息/任务
 ↓
继续运行
 ↓
...
```

**不要设计成：**

```text
完成任务
 ↓
自动退出
```

这是 Team 与一次性 Subagent 的重要区别。

---

# 九、下线协议

下线协议采用：

```text
Main → Agent
Agent → Main
```

的方向，而不是 Agent 主动要求自己退出。

基本流程：

```text
Main
 │
 │ 判断当前不再需要 Agent A
 ↓
Agent A
 │
 │ 检查自己的当前工作和消息
 │
 ├── 仍需要工作
 │      ↓
 │   拒绝下线
 │      ↓
 │   继续 alive
 │
 └── 当前可以结束
        ↓
      同意下线
        ↓
      停止线程
        ↓
Main 确认
        ↓
成员状态更新为 offline
```

这里不强制要求采用 TCP 三次握手/四次挥手的形式。

核心要求只有：

> Main 发起下线意图 → Agent 最终确认 → Agent 实际停止 → Main 更新最终状态。

具体需要几个消息、是否需要 ACK、如何保证线程安全，由实现自行决定。

不要为了模拟 TCP 而无意义增加通信次数。

---

# 十、关于“Agent 是否真的还需要工作”

这里必须区分两种判断：

### 确定性判断

交给代码：

```text
是否存在消息
成员是否存在
ID 是否有效
线程是否存活
成员当前逻辑状态
```

### 非确定性判断

交给模型：

```text
当前任务是否真正完成
某条消息是否仍需要自己处理
当前协作是否已经结束
Main 是否认为整个 Team 已经不再需要自己
```

不能要求纯代码完全判断：

> “这个 Agent 以后是不是永远不会再被需要。”

这是业务/任务层面的判断。

同时也不能要求模型负责底层生命周期事实，例如：

> “我的线程是不是已经停止。”

这些属于 Runtime。

---

# 十一、成员列表的可见性

Team 成员信息不应该默认全部塞入每个 Agent 的 Prompt。

可以考虑提供一个工具，让 Agent 在需要协作时主动查询当前 Team：

```text
team_list()
```

或者等价能力。

其他 Team 成员：

### 可以读取：

```text
当前有哪些成员
成员 ID
成员名称
成员状态
```

### 不能修改：

```text
成员状态
成员生命周期
成员创建/删除
```

成员表的写权限属于 Main Agent / Team Runtime。

这样可以支持真正的 Agent-to-Agent 协作：

```text
Agent A
  ↓
查看 Team
  ↓
发现 Agent B
  ↓
使用 B 的 ID
  ↓
发送消息给 B
```

而不是让每个 Agent 只能依赖 Main。

---

# 十二、不要改变已有架构中已经验证良好的部分

本次修改应该是增量修改，不要重新设计整个 Agent Loop。

尤其保留已有的：

```text
MessageBus + Condition.wait()
```

带来的：

> Agent idle 时真正阻塞、无 busy loop、低 CPU 占用。

不要为了生命周期管理而加入：

```text
持续轮询
固定时间唤醒
idle 超时自动退出
```

Team 成员可以无限期 idle。

同时，不要求本次修改解决：

> 正在执行一个同步 LLM/tool 调用时立即中断 Agent。

当前语义仍然允许：

```text
当前 run()
    ↓
完成当前执行边界
    ↓
再处理控制消息
```

不要为了退出协议引入复杂的任务取消系统。

---

# 十三、本次实现的边界

### 必须实现

1. Agent 使用唯一 ID，而不是 name 作为通信身份。
2. 增加 Team 成员注册/管理机制。
3. 成员状态与物理线程状态分离。
4. 生命周期控制消息与普通业务消息分离。
5. `/exit` 不再作为普通字符串协议处理。
6. Main 可以主动请求成员下线。
7. Agent 收到下线请求后必须进行最终确认，不能无条件立即退出。
8. Agent 完成当前任务后继续 alive/idle，而不是自动退出。
9. Main 最终能够确认成员已经退出并同步状态。
10. Team 成员可以读取当前 Team 成员信息。

### 不要求本次实现

1. 复杂可靠消息协议。
2. ACK/重试/超时系统，除非当前实现确实需要。
3. Agent 之间的复杂调度算法。
4. 自动判断“未来是否永远需要某 Agent”。
5. 任务取消/强制线程终止机制。
6. idle 超时退出。
7. 大规模并发优化。
8. 对整个 Agent Loop 进行重新架构。

---

# 十四、实现原则

**不要为了实现上述目标而过度设计。**

优先在现有：

```text
ClaudeMini
Agent Loop
Message
MessageBus
Main
```

架构上增量修改。

可以新增必要的 Team 成员管理模块，但不要把已有的 Agent Loop、MessageBus 等重新设计成完全不同的架构。

具体：

* 类怎么设计
* 方法叫什么
* 状态如何保存
* 锁如何使用
* 消息如何排队
* 下线协议具体几步
* 如何实现线程同步

这些由你根据现有代码自行决定。

**但必须保持以上职责边界和行为语义，不要为了方便实现而改变设计目标。**

实现完成后，应重点测试：

1. 普通 Agent → Agent 通信。
2. 多 Agent 并发通信。
3. Agent 完成任务后继续 idle。
4. Main 主动请求 Agent 下线。
5. Agent 有待处理消息时拒绝下线。
6. Agent 无待处理工作时同意下线。
7. Agent 实际线程停止后 Main 状态正确更新。
8. Agent 退出后新建同名 Agent，不会收到旧 Agent 的消息。
9. 控制消息不会进入普通 LLM 消息流。
10. Agent idle 时仍保持阻塞等待，不产生 busy loop。
