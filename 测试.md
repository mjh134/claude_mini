# Agent Team Runtime 测试文档

## 一、测试目标

当前 Agent Team 实现包含：

* `Message`
* `MessageBus`
* `ClaudeMini.run_forever()`
* `team_spawn`
* `send_message`
* `team_list`
* Main Agent 的 `has_team_message()` / `collect_team_messages()`
* 用户输入线程 + Queue

本次测试不修改架构，主要验证当前实现的运行时行为。

重点确认：

1. Agent 是否能够在 `idle` 状态等待消息。
2. 收到消息后是否进入 `work`。
3. 工作完成后是否重新进入 `idle`。
4. Agent 工作期间收到的新消息是否会丢失。
5. Agent 工作期间收到多条消息时是否能够全部处理。
6. Agent ↔ Agent 双向通信是否正常。
7. Main Agent 是否能够在用户没有输入的情况下收到 Team 消息。
8. Main Agent 是否会因为正在执行 `run()` 而暂时无法处理 Team 消息。
9. `/exit` 是否能够正常终止 Teammate。
10. 是否存在消息丢失、重复处理、死锁、CPU 空转等问题。

---

# 二、测试原则

测试时不要直接修改 Agent Team 实现。

如果发现问题：

1. 先记录实际行为。
2. 再解释为什么发生。
3. 判断这是：

   * 当前设计预期行为
   * 实现 Bug
   * 尚未实现的功能
4. 最后再提出最小修改建议。

不要为了让测试通过而直接修改代码。

---

# 三、测试 1：最基本的 Alice → Bob

## 操作

让 Main Agent 创建两个成员：

```text
创建 alice 和 bob 两个团队成员。

alice 的任务：
给 bob 发送消息：“hello bob”。

bob 收到后回复 alice：
“hello alice”。

alice 收到回复后告诉我：
“alice 已收到 bob 的回复”。
```

## 观察

记录完整消息流：

```text
main → alice
alice → bob
bob → alice
```

确认：

* Alice 能否发送给 Bob。
* Bob 是否能够被唤醒。
* Bob 是否能够回复 Alice。
* Alice 是否能够再次被唤醒。
* 两个 Agent 是否最终回到 `idle`。

---

# 四、测试 2：Teammate 工作期间收到消息【重点】

这是本次最重要的测试。

## 操作

创建 Alice：

```text
创建 alice。

让 alice 执行一个明显需要一段时间的任务：
执行 bash sleep 5。

任务完成后回复我：
“第一阶段完成”。
```

Alice 开始执行任务以后，立即让 Main：

```text
给 alice 发送第二条消息：

“这是第二阶段任务，请回复：收到第二阶段任务。”
```

## 观察

重点观察：

```text
alice
  │
  ├── receive()
  │
  ▼
 work
  │
  ├── sleep 5
  │
  │   此时第二条消息到达
  │
  ▼
 第一阶段完成
  │
  ▼
 run() 返回
  │
  ▼
 receive()
  │
  ▼
 第二条消息被取出
```

确认：

* 第二条消息是否丢失。
* Alice 是否会在第一阶段工作期间被强制中断。
* 第一阶段结束后是否能收到第二条消息。
* 第二条消息是否最终被处理。
* Alice 是否重新进入 `idle`。

### 重点回答

测试完成后明确回答：

> 当前 MessageBus 是否支持“中断正在工作的 Agent”？

以及：

> 当前实现实际上采用的是“消息排队，Agent 在下一个工作周期处理”，还是“消息立即打断当前工作”？

---

# 五、测试 3：工作期间收到多条消息

创建 Alice：

```text
让 alice 执行：

bash sleep 5
```

在 Alice 工作期间连续发送：

```text
消息 1：任务 A
消息 2：任务 B
消息 3：任务 C
```

测试结束后检查：

```text
A
B
C
```

是否全部被处理。

同时检查：

* 顺序是否保持。
* 是否出现重复。
* 是否出现丢失。
* 是否一次性 drain 多条消息。
* Alice 是否需要执行多次 `run()`。

---

# 六、测试 4：Main Agent 收到 Team 消息时没有用户输入

创建 Alice：

```text
创建 alice。

让 alice 等待 2 秒，然后主动给 main 发送：

“这是 Alice 主动发送给 Main 的消息。”
```

然后 Main 不输入任何内容。

观察：

```text
用户什么都不输入
        │
        ▼
input thread 阻塞
        │
        │
        └──────────────┐
                       │
                    Main Runtime
                       │
                  检查 Team 消息
                       │
                       ▼
                  收到 Alice 消息
```

确认：

* Main 是否能够在没有用户输入的情况下处理 Alice 的消息。
* 消息到达后大约多久被处理。
* 是否仍然必须等待用户输入才能处理。
* `timeout=1` 对响应延迟有什么影响。

---

# 七、测试 5：Main 正在执行工作时收到 Team 消息

这是另一个重点测试。

让 Main 执行一个明显需要较长时间的任务，例如：

```text
执行 bash sleep 5。
```

在 Main 工作期间，让 Alice 给 Main 发送：

```text
“Alice 的消息。”
```

观察：

```text
Main
 │
 ▼
run()
 │
 ├── bash sleep 5
 │
 │     Alice → Main
 │
 ▼
run() 当前工作完成
 │
 ▼
下一轮检查 MessageBus
 │
 ▼
处理 Alice 消息
```

回答：

> Main 在 `run()` 执行期间能否处理 Team 消息？

如果不能，说明当前：

```text
has_team_message()
```

只是 Main Runtime 的唤醒探针，而不是能够真正打断 `run()` 的异步中断机制。

---

# 八、测试 6：双向并发通信

创建：

```text
alice
bob
```

给 Alice：

```text
给 bob 发送：“来自 alice 的消息”
```

给 Bob：

```text
给 alice 发送：“来自 bob 的消息”
```

尽可能让两个消息同时发生。

观察：

* MessageBus 是否出现竞争问题。
* 两个 Agent 是否都能正确收到消息。
* 是否发生死锁。
* 是否有消息被错误 Agent 获取。

---

# 九、测试 7：Agent 空闲时 CPU 占用

启动两个长期存在的 Teammate，但不给它们发送消息。

观察：

```text
alice → idle
bob   → idle
```

检查：

* CPU 是否基本不占用。
* 是否存在 while True 空转。
* `Condition.wait()` 是否真正阻塞。

然后发送一条消息：

```text
main → alice
```

确认 Alice 能够被唤醒。

---

# 十、测试 8：退出

创建：

```text
alice
bob
```

然后：

```text
停止 alice。
停止 bob。
```

观察：

```text
alice → exit
bob   → exit
```

确认：

* Agent 是否退出 `run_forever()`。
* `state` 是否变成 `exit`。
* 线程是否结束。
* 是否还会继续处理新消息。

---

# 十一、测试 9：退出消息与普通消息同时到达

给 Alice 连续发送：

```text
普通消息 A
/exit
普通消息 B
```

观察最终行为。

重点确认：

* Alice 是否先处理 A。
* `/exit` 是否导致退出。
* B 是否仍然留在 MessageBus 中。
* B 是否会被重新创建的同名 Agent 误收到。

如果发现这种情况，不要立即修改代码，先记录当前语义。

---

# 十二、测试 10：消息顺序

连续发送：

```text
1
2
3
4
5
```

让同一个 Agent 接收。

确认最终收到顺序：

```text
1 → 2 → 3 → 4 → 5
```

检查 `MessageBus.messages` 的 FIFO 行为是否符合预期。

---

# 十三、最终测试报告

测试完成后按照下面格式总结。

## 1. 正常功能

列出已经验证正常的功能。

## 2. 实际行为

重点说明：

* idle → work → idle
* 工作期间收到消息
* 多消息
* Main Agent 收消息
* Agent 双向通信
* shutdown

## 3. 发现的问题

每个问题说明：

```text
问题：
实际行为：
预期行为：
原因：
是否属于当前设计预期：
```

## 4. 当前架构的一个关键结论

重点回答：

> MessageBus 当前负责什么？

> run_forever() 当前负责什么？

> Main Agent 的 input thread 当前解决什么问题？

> 当前系统能否中断一个正在执行 run() 的 Agent？

> 当前 Team 通信属于“异步消息传递”，还是“实时中断式通信”？

## 5. 修改建议

只提出必要的最小修改。

不要为了测试而进行大规模重构。
