# 版本控制说明

2026-09-18 建库。在此之前这个项目没有版本控制,一次代码回退只能靠翻会话记录、
按 `cat -n` 的行号把原文"挖"回来 —— 这份文档是为了不再有第二次。

仓库是**纯本地**的,没有远程,没有推送。

> **git 和 GitHub 是两回事。** git 是装在本机的工具,不联网、不需要账号就能用全部功能;
> GitHub / Gitee 只是"放 git 仓库的网站"。要把仓库传上去,得先告诉 git 传到哪儿:
> `git remote add origin <地址>` 再 `git push`。那是对外动作,本项目没做,也不打算未经确认就做。

---

## 一、三个区域 —— 理解 git 就看这一张图

```
   工作区                 暂存区                  仓库
 (磁盘上的文件)  --add-->  (.git/index)  --commit-->  (历史快照)
   你在这改东西           下一次提交的草稿箱          一次次的提交
```

**暂存区(英文 staging area,也叫 index / 索引)= 下一次提交的草稿箱。**

它存在的意义是**让你只提交一部分改动**:同时改了 5 个文件,但只想把其中 2 个做成一次提交,
那就只 `add` 那 2 个。没有这一层,一提交就得全提交。

**关键:暂存区里放的是"快照",不是"这个文件"。** 同一个文件可以在两个地方同时存在两份不同内容:

```bash
echo 第一行 > a.txt
git add a.txt          # 此刻暂存区里的 a.txt 只有第 1 行
echo 第二行 >> a.txt    # 磁盘上的 a.txt 变成两行
git status --short
#  AM a.txt            # A = 暂存区里是新加的;M = 磁盘上又改了
git commit -m "..."
#  M a.txt             # 提交进去的只有第 1 行,第 2 行还留在工作区,等下次提交
```

`AM` 这个状态最能说明问题:暂存区里的 `a.txt` 和磁盘上的 `a.txt` **同时存在、内容不同**。
所以 `add` 是"添加**快照**",不是"添加文件"。

### `git status --short` 那两个字母怎么看

左边一位 = 暂存区 vs 上次提交,右边一位 = 工作区 vs 暂存区。

| 符号 | 意思 |
|---|---|
| `??` | 新文件,git 还不认识它(未跟踪) |
| `A ` | 新文件已经进了暂存区 |
| ` M` | 已跟踪的文件内容改了,还没 add |
| `M ` | 改动已经进了暂存区 |
| ` D` / `D ` | 已跟踪的文件被删了 / 删除已进暂存区 |
| `AM` `MM` | 两边都有变化(加了之后又改) |

**`git status` 不是"删除记录"** —— 它报的是当前**所有**还没提交的变化:新增、修改、删除都报。

### 两个常见误解

**① `git add` 本身不默认任何东西,你得说加哪个:**

| 命令 | 效果 |
|---|---|
| `git add main.py` | 只加 main.py |
| `git add -A` | 当前目录下**所有**变化(新增 / 修改 / 删除都算) |

但**不是"所有文件"** —— 被 `.gitignore` 挡住的(`.env`、`tool_result/`)永远加不进去,
`git add -A` 也加不进去。这是故意的。

**② `git commit` 自己不加任何东西**,它只把暂存区里已有的打包。暂存区空着,
它直接拒绝(`nothing to commit`)。所以流程严格是:

```
改文件  →  git add(挑要提交的)  →  git commit(打包暂存区)  →  进历史
```

偷懒写法 `git commit -a -m "..."`:自动把**已跟踪**文件的改动加进来再提交,省掉 `add`。
但它**管不到新文件**(`??` 那些) —— 新文件必须至少 `git add` 过一次。

---

## 二、日常三条

```bash
git status              # 先看这个:哪些文件改了、哪些还没提交
git add -A              # 把改动放进暂存区(-A = 包括新增和删除)
git commit -m "改了什么"  # 存成一个快照(提交信息用中文没问题,已经配好了)
```

`git status` 是最该常敲的一条。**动手改代码之前和之后各敲一次**,心里就有数。

只想提交部分文件:

```bash
git add main.py HOOKS.py
git commit -m "..."
```

---

## 三、回退:三种情形

### 情形 1:改了,还没提交,想扔掉这些改动 ← 最常用

```bash
git status                      # 先确认要扔的是哪些
git checkout -- main.py         # 单个文件退回上次提交的样子
git checkout -- .               # 整个工作区全扔(危险,先看清楚 status)
```

`git checkout -- <文件>` 是**按字节**还原的 —— 行尾、末尾有没有换行,都跟提交时一模一样。
(已演练验证:CRC 校验前后一致。)

### 情形 2:已经提交了,想撤销某次提交

```bash
git log --oneline               # 找到要撤的那次的 sha
git revert <sha>                # 生成一个"反向提交",把那次改动抵消掉
```

用 `revert` 而不是 `reset`,因为 `revert` **不改写历史**,安全,出错也好再 revert 回来。

### 情形 3:想看/取回某个历史版本的文件

```bash
git show <sha>:main.py                    # 只看看,不落盘
git show <sha>:main.py > 别处.py           # 导出到别的文件
git checkout <sha> -- main.py             # 用历史版本覆盖当前文件(会进暂存区)
```

**取回已删除的文件**(比如 `console.py`)就是这么取:

```bash
git log --diff-filter=D --name-only       # 列出删除过哪些文件
git show c5e39a4^:console.py > console.py # ^ 表示"那次删除提交的父提交"
```

---

## 四、打标签:给"能用的版本"起名字

比记 sha 好得多。跑到一个自己满意的状态,就打个标签:

```bash
git tag 能用-事件驱动输入版
git tag                                       # 列出所有标签
git checkout 能用-事件驱动输入版 -- 某个文件     # 从标签取文件
```

**标签打在“提交”上** —— 不是文件,也不是工作区。它本质就是给某个提交哈希起个别名,
所以**必须先 commit 才有东西可打**。自己验一下,下面两行输出的是同一个哈希:

```bash
git rev-parse 能用-事件驱动输入版
git rev-parse HEAD
```

以后要回退,直接说标签名,不用去翻一长串 sha。

---

## 五、看历史

```bash
git log --oneline                    # 一行的提交列表
git log --oneline -- main.py         # 只看这个文件的改动历史
git show <sha>                       # 某次提交具体改了什么
git diff                             # 还没提交的改动内容
git diff HEAD~1                      # 和上一次提交比
```

---

## 六、两条铁律

### 1. `.env` 和 `tool_result/` 永远不进库

`.gitignore` 已经挡住了。**不要用 `git add -f` 强行加它们。**

- `.env` —— 里面有真实的 API 密钥。
- `tool_result/` —— 185 个工具调用缓存,其中 3 份是整份 `.env` 的 dump,**含密钥明文**。
  一旦提交,密钥就永久留在 git 历史里,删都删不干净。

提交前想确认一下:

```bash
git status --porcelain          # 输出里不该出现 .env 或 tool_result
git ls-files | grep -i env      # 只该看到 .env.example
```

### 2. `core.autocrlf` 必须保持 `false`

```bash
git config core.autocrlf        # 应该输出 false;不是的话立刻改回来
git config core.autocrlf false
```

**为什么**:这个项目的行尾是**故意不统一**的 —— 12 个 `.py` 是 CRLF、7 个是 LF,
`.md` 也是混的。这是历史形成的现状,不是待修的毛病。系统级配置
(`C:\Program Files\Git\etc\gitconfig`)里 `core.autocrlf` 是 `true`,
它会在 `git add` 时把 CRLF 转成 LF 存库、检出时又把文件改写成 CRLF ——
**静默改写文件内容**,"按字节回退"就失效了。

所以本仓库特意覆盖成 `false`,并且 `.gitattributes` 里写明了不要加 `text=auto`。

> **换机器 clone 时**:到那台机器上也要执行一次 `git config core.autocrlf false`,
> 否则行尾会被改写。这条配置是本地的,没有跟仓库一起提交。

---

## 七、当前的历史

```
84ec3ec  删除测试数据与测试文档(sales_report、budget_q4、stress 等 41 个文件)
17f0645  加一份 版本控制.md        ← 标签「能用-事件驱动输入版」打在这一条上
c5e39a4  删除 console.py(接管控制台方案已放弃)
65e5e82  项目基线:事件驱动输入循环版本
1974933  加入 .gitignore / .gitattributes,并锁定不做行尾规范化
```

`console.py`(接管控制台的 462 行方案)在 `65e5e82` 里,工作区已删除,
但代码永久留在历史里 —— 哪天想捡回那条路,用第三节的取回命令就能拿回来。

最新那条提交(84ec3ec)里删掉的 41 个测试文件(`sales_report/`、`budget_q4/`、
`stress/`、`测试.md`、`测试报告.md`、`设计.md`)同样都在历史里,
`git checkout 17f0645 -- <路径>` 就能取回。
